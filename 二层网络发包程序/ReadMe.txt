备注：
本程序主要使用原始套接字，进行二层网络数据包的发送

在该程序中如果将 源MAC地址改为网关GW地址, 那么在该发送机器和目标机器（目的MAC）网络拓扑之间的所有交换机都将受到欺骗，即把网关GW所在的交换机端口重写为运行该程序的电脑所在的交换机端口，明显这是错误的。如果大量的发包，使其数据包所经过的交换机重写速度大于交换机自身的ARP发现包的重写速度，就会使交换机把网关所在端口错误的定向为源主机所在的端口，这样便使所有在交换机之下的电脑都无法与网关正确互通。   
